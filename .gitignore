*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
build/

# Secrets — never commit provider keys. See llm/README.
/app/src/main/assets/secrets.properties
secrets.properties

# Release signing — never commit the keystore or its passwords.
keystore.properties
*.jks
*.keystore
