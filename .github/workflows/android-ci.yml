name: Android CI

# Real build verification: GitHub-hosted Ubuntu runners ship the full Android SDK, so this
# compiles the app on every push and PR — the check we cannot run in the authoring sandbox.

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Assemble debug APK
        run: ./gradlew assembleDebug --stacktrace

      # Verify the minified/shrunk release build too — this is what exercises the R8/ProGuard
      # rules. CI has no keystore, so the APK is unsigned, but R8 still runs. A release-only
      # shrinker failure would otherwise only surface at store-upload time.
      - name: Assemble release APK (unsigned, R8)
        run: ./gradlew assembleRelease --stacktrace

      - name: Android Lint
        run: ./gradlew lintDebug --stacktrace
        continue-on-error: true

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: warn

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: warn
